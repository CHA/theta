import { SwiperOptions } from 'swiper';

export class FreeModeSwiperOptions {
  static config: SwiperOptions = {
    slidesPerView: 2.4,
    spaceBetween: 0,
    freeMode: true,
    pagination: false
  };
}
